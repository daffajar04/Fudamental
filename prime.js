for (let i = 1; i <= 10; i++) {
    let devider = 0}
    for (let j = 1; j <= i; j++) {
        if (i % j == 0) devider
     }
     console.log(devider == 2 ? '${i} is a prime number' : '${j} is not a prime number')
